/**
 * Funciones utilitarias de validación de formularios
 * (correo institucional, formato de archivos CSV, campos requeridos).
 * Pendiente de implementación: solo estructura.
 */

export function esCorreoInstitucionalValido(correo) {
  // Pendiente
  return true;
}
